name: Publish to https://lmno.lol/crisidev

on:
  push:
    branches:
      - main

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Publish blog
        run: |
          ${PWD}/scripts/deploy.sh -p ${secrets.LMNO_LOL_API_KEY}
